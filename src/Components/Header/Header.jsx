import React from 'react';
import { Title } from './Title';
import { NavBar } from './NavBar';

export const Header = () => {
  return (
    <header>
      <Title />
      <NavBar />
    </header>
  );
};
